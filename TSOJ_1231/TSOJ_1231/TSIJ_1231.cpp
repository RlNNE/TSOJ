/*TSOJ_1231*/
/*多组输入。每组输入为一个仅由小写字母组成的字符串。输入数据
保证有解，即至少有一个字母仅出现一次。字符串的长度不超过 255。*/

#include<stdio.h>
#include<stdlib.h>
#include<string.h>

#define M 100

int main(void) {
	char a[M];
	int i, t, b, c;
	 
	return 0;
}